services:
  - type: web
    name: scam-detector
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    # IMPORTANT: Replace the Cohere API key with an environment variable
    # This prevents the key from being exposed publicly
    envVars:
      - key: uEhP9GILjS4SE7mrwGkUOr8BYDoXGeJwOpVejMXp
        sync: false
